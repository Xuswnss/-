#!/bin/sh
# Backend 빌드/실행: 시스템에 Gradle이 있으면 사용합니다.
# 없으면 IDE에서 BackendApplication 실행 또는 'gradle wrapper' 후 ./gradlew 사용.
set -e
if command -v gradle >/dev/null 2>&1; then
  exec gradle "$@"
fi
echo "Gradle이 PATH에 없습니다. Gradle을 설치하거나 IDE에서 BackendApplication을 실행하세요."
exit 1
